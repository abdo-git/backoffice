const initState = {
    file:{
        name:'',
        tags:[],
        onglets:[],
    }
};

const fileReducer = (state = initState, action) => {
  switch (action.type) {
    case '':
      break;
    default:
      break;
  }
  return state;
};

export default fileReducer;
